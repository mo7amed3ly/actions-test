name: CI-CD
env:
  ARTIFACT_NAME: myFiles
jobs:
  Test:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Test
        runs: python test.py
    
